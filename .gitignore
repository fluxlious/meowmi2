# Session notes for Claude. Local only, by choice — note that this means a
# fresh clone on another machine will not have it.
CLAUDE.md

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Nothing is built or installed — Three.js loads from a CDN via the import map
# in index.html. If that ever changes, node_modules goes here.
node_modules/
